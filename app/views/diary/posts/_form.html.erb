<%= form_with(model: diary_post) do |form| %>
  <% if diary_post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(diary_post.errors.count, "error") %> prohibited this diary_post from being saved:</h2>

      <ul>
        <% diary_post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
